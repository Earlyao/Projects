请将主机（CO）和路由（RO）的P1.3、P1.5拉高重启进入设置模式方能响应配置程序。模块串口TX为P1.6，RX为P1.7。需与电脑串口进行正确的连接。

终端（EN）无需拉高IO口，但需要选择“终端（EN，带唤醒）”项。

本配置工具只为方便量产时的批量配置，具体的串口配置命令与使用以《ZAuZx_T 系列低功耗Zigbee 无线网络串口透传解决方案》为准。如系统文件不支持无法运行，可先跳过本配置程序，不影响模块的学习使用。